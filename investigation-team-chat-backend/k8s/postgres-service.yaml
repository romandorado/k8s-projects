apiVersion: v1
kind: Service
metadata:
  name: postgres-chat-svc
  namespace: investigation-team-frontend
spec:
  type: ClusterIP
  selector:
    app: postgres-chat
  ports:
    - port: 5432
      targetPort: 5432
