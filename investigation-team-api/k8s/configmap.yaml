apiVersion: v1
kind: ConfigMap
metadata:
  name: investigation-team-config
  namespace: investigation-team
  labels:
    app: investigation-team-api
data:
  APP_NAME: "InvestigationTeam API"
  APP_VERSION: "1.0.0"
  DEBUG: "false"
  LOG_LEVEL: "info"
  # Configuración de base de datos (para futuro)
  # DB_HOST: "postgres-svc"
  # DB_PORT: "5432"
  # DB_NAME: "investigation_team"
