apiVersion: v1
kind: Service
metadata:
  name: terraria-agent
  namespace: terraria
  labels:
    app: terraria-agent
spec:
  type: ClusterIP
  selector:
    app: terraria-agent
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
      name: http
