apiVersion: v1
kind: Service
metadata:
  name: supermarket-postgres-svc
  namespace: supermarket
  labels:
    app: supermarket-postgres
spec:
  type: ClusterIP
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
      name: postgres
  selector:
    app: supermarket-postgres
