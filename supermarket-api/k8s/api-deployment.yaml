apiVersion: apps/v1
kind: Deployment
metadata:
  name: supermarket-api
  namespace: supermarket
  labels:
    app: supermarket-api
spec:
  replicas: 2
  selector:
    matchLabels:
      app: supermarket-api
  template:
    metadata:
      labels:
        app: supermarket-api
    spec:
      containers:
        - name: api
          image: docker.io/library/supermarket-api:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8000
              name: http
          env:
            - name: ASPNETCORE_ENVIRONMENT
              value: "Production"
            - name: ConnectionStrings__DefaultConnection
              value: "Host=supermarket-postgres-svc;Port=5432;Database=supermarket;Username=postgres;Password=postgres"
          resources:
            requests:
              memory: "128Mi"
              cpu: "100m"
            limits:
              memory: "256Mi"
              cpu: "200m"
          readinessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 15
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8000
            initialDelaySeconds: 30
            periodSeconds: 10
