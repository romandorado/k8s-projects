apiVersion: v1
kind: Service
metadata:
  name: supermarket-api
  namespace: supermarket
  labels:
    app: supermarket-api
spec:
  type: LoadBalancer
  ports:
    - port: 80
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: supermarket-api
